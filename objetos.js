const objetoPessoa = {
   nome:"José",
   idade: 32
};

const  cliente = {
    nome:"Andre",
    idade:"32",
    cpf:"111222333",
    email:"andre@gmail.com"
};

console.log(`o nome do clinte é ${cliente.nome}
    e essa pessoa tem ${cliente.idade} anos.`);
console.log(`os 3 primeiros digitos do CPF são ${cliente.cpf.substring}`)